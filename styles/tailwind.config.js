/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html",          // Include HTML files in the ./src/ directory and its subdirectories
    "./html/**/*.html",         // Include HTML files in the ./html/ directory and its subdirectories
  ],
  
}


